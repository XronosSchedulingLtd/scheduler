<% if @minimal && !@minute %>
<a class="close-reveal-modal">&#215;</a>
<% end %>

<div id="editing_form">
<%= render 'form' %>
</div>

<% if !@minimal && !@minute %>
<%= link_to 'Show', @event %> |
<%= link_to 'Back', events_path %>
<% end %>
