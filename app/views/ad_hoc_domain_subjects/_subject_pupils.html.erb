<% pupil_courses = ad_hoc_domain_staff.pupils_for(subject_pupils) %>
<div id='ahd-subject-pupils-u<%= subject_pupils.id %>'>
  <% if pupil_courses.size == 0 %>
    <%= ahd_row(
      contents: {
        no_pupils: "None defined yet."
      }) %>
  <% end %>
  <%= render pupil_courses.sort %>
</div>

