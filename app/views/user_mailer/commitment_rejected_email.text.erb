
Unfortunately, <%= @rejecter %> has declined your request to use:

  <%= @element.name %>
  
for:

  "<%= @event.body %>" on <%= @event.starts_at.strftime("%d/%m/%Y") %>
  
giving the following reason:

  <%= @reason %>

