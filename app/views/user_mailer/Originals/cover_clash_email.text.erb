Possible cover issues - <%=
  now = Time.zone.now
  now.strftime("%a #{now.day.ordinalize} %b, %Y %H:%M")
%>

<% if @clashes.size > 0 %>
  Automatic processing of the cover records from <%= Setting.current_mis_name %> has
  thrown up the following possible clashes.

<%= render @clashes %>
<% end %>

<% if @oddities.size > 0 %>
  Automatic processing of the cover records from <%= Setting.current_mis_name %> has
  thrown up the following possible oddities.

<%= render @oddities %>
<% end %>

