
<%= @approver %> has approved your request for:

  <%= @element.name %>
  
for:

"<%= @event.body %>" on <%= @event.starts_at.strftime("%d/%m/%Y") %> at <%= @event.starts_at.strftime("%H:%M") %>

<% if @complete %>
Your event is now complete.
<% else %>
The following items requested for your event are still awaiting
approval:
<% @pending_elements.each do |e| %>
  <%= e.name %>
<% end %>
<% end %>
