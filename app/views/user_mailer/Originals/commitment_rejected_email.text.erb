
Unfortunately, <%= @approver %> has identified a problem with your
request to use:

  <%= @element.name %>
  
for:

  "<%= @event.body %>" on <%= @event.starts_at.strftime("%d/%m/%Y") %>
  
giving the following reason:

  <%= @reason %>

Is it possible to adjust your requirements?
