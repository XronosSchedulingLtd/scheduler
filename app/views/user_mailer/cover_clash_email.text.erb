Possible cover issues - <%=
  now = Time.zone.now
  now.strftime("%a #{now.day.ordinalize} %b, %Y %H:%M")
%>

  Automatic processing of the cover records from SchoolBase has
  thrown up the following possible clashes.

<%= render @clashes %>
