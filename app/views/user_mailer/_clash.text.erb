<%
  if clash.cover_commitment.event.starts_at.to_date != @current_date
    @current_date = clash.cover_commitment.event.starts_at.to_date
%>
  <%= @current_date.strftime("%a %-d %b %y") %>

<%
  end
%>
<% if clash.cover_commitment.covering %>
<%= clash.cover_commitment.element.entity.initials %> covering <%= clash.cover_commitment.covering.element.entity.initials %> for <%= clash.cover_commitment.event.body %>
<% else %>
<%= clash.cover_commitment.element.entity.initials %> taking <%= clash.cover_commitment.event.body %>
<% end %>
From <%= clash.cover_commitment.event.starts_at.strftime("%H:%M") %> to <%= clash.cover_commitment.event.ends_at.strftime("%H:%M") %>
<%= clash.cover_commitment.element.entity.initials %> also has <%= clash.clashing_commitment.event.body %>
<% if clash.clashing_commitment.event.all_day %>
All day
<% else %>
From <%= clash.clashing_commitment.event.starts_at.strftime("%H:%M") %> to <%= clash.clashing_commitment.event.ends_at.strftime("%H:%M") %>
<% end %>

