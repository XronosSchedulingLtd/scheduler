The following resources in Scheduler currently have requests
awaiting your approval.

<% @user_set.pending_sets.each do |ps| %>
  <%= ps.element.name %>
<%   ps.pending.each do |commitment| %>
    <%= commitment.event.owners_initials %> proposes "<%= commitment.event.body %>" on <%= commitment.event.starts_at_text %>
<%   end %>
<% end %>
