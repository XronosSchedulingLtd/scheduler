window.ahdUpdate([
  {
    action: 'new_link',
    subject_id: <%= @ad_hoc_domain_subject.id %>,
    staff_id: <%= @ad_hoc_domain_staff.id %>,
    staff_listing: "<%=j(
    render(
      partial: 'ad_hoc_domain_subjects/subject_inner',
      object: @ad_hoc_domain_subject,
      locals: { nested: false }
    )) %>"
  },
  {
    action: 'add_staff',
    position: <%= position %>,
    cycle_id: <%= @ad_hoc_domain_cycle.id %>,
    html: '<%=j render object: @ad_hoc_domain_staff, partial: 'ad_hoc_domain_staffs/staff_outer', locals: { nested: false } %>'
  },
  {
    action: 'clear_errors'
  }
]);

