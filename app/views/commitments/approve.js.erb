<% if @event %>
window.replaceCommitments(
  "<%=j render partial: "events/approvable_commitments",
               object: @approvable_commitments
  %>")
$('#pending_count').html("<%= current_user.permissions_pending %>")
<% end %>
