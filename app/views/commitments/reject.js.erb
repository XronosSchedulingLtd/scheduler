<% if @event %>
window.replaceCommitments(
  "<%=j render partial: "events/resource",
               object: @event,
               locals: {editing: @editing}
  %>")
$('#pending_count').html("<%= current_user.permissions_pending %>")
<% end %>
